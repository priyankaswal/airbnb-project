<nav class="bg-white shadow-md fixed top-0 left-0 w-full p-4 z-10">
  <div class="max-w-4xl mx-auto flex justify-between items-center">
    <!-- Left Section -->
    <div class="space-x-4">
      <a
        href="/"
        class="px-4 py-2 bg-blue-600 text-white font-semibold rounded hover:bg-blue-800"
      >
        airbnb
      </a>

      <% if(isLoggedIn) { %> 
        <% if(user.userType === 'guest') { %>
          <a
            href="/homes"
            class="px-4 py-2 bg-blue-600 text-white font-semibold rounded hover:bg-blue-800"
          >
            Homes
          </a>

          <a
            href="/favourites"
            class="px-4 py-2 bg-blue-600 text-white font-semibold rounded hover:bg-blue-800"
          >
            Favourites
          </a>
        <% }else{ %>
          <a
            href="/host/add-home"
            class="px-4 py-2 bg-blue-600 text-white font-semibold rounded hover:bg-blue-800"
          >
            Add Home
          </a>
          <a
            href="/host/host-homes"
            class="px-4 py-2 bg-blue-600 text-white font-semibold rounded hover:bg-blue-800"
          >
            Host Homes
          </a>
        <% } %>
      <% } %>
    </div>

    <!-- Right Section -->
    <div class="flex space-x-4">
      <% if(!isLoggedIn) { %>
      <a
        href="/signup"
        class="px-4 py-2 bg-green-600 text-white font-semibold rounded hover:bg-green-800"
      >
        Signup
      </a>
      <a
        href="/login"
        class="px-4 py-2 bg-blue-600 text-white font-semibold rounded hover:bg-blue-800"
      >
        Login
      </a>

      <% } else { %>
      <form action="/logout" method="post">
        <input
          type="submit"
          value="Logout"
          class="px-4 py-2 bg-red-600 text-white font-semibold rounded hover:bg-red-800"
        />
      </form>
      <% } %>
    </div>
  </div>
</nav>
